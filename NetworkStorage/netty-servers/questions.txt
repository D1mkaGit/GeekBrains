Проверял все на 600мб файлах, все передается.
Была проблема утечки памяти, но решилась релизом ((ByteBuf) msg).release(); в channelRead
И вопрос - ctx.writeAndFlush(buf) гарантировано релизит буфер или нет? А то у меня данные пропадали иногда, сделал вот такую конструкцию
ctx.writeAndFlush(buf).sync();
if (buf.refCnt() > 0) buf.release();
Проблемы пропали, но мне кажется дело там было в чем-то другом вообще.
===
Отвязка логики сети от GUI
===
Proto странности